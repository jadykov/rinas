---
import '../styles/global.css';
import Metrics from '../components/Metrics.astro';
import { TELEGRAM_LINK } from '../lib/site';

interface Props {
  title: string;
  description?: string;
  image?: string;
}

const { title, description, image } = Astro.props;
const fullTitle = title ? `${title} — Мастерская кашпо` : 'Мастерская кашпо';
const canonical = `${Astro.url.origin}${Astro.url.pathname}`;
---
<!doctype html>
<html lang="ru">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{fullTitle}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonical} />
    <meta property="og:site_name" content="Мастерская кашпо" />
    <meta property="og:type" content={image ? 'article' : 'website'} />
    <meta property="og:title" content={fullTitle} />
    {description && <meta property="og:description" content={description} />}
    <meta property="og:url" content={canonical} />
    {image && <meta property="og:image" content={image} />}
    <Metrics />
  </head>
  <body>
    <header class="header">
      <div class="container header__inner">
        <a class="logo" href="/">
          <svg class="logo__mark" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
            <path d="M12 15c0-6-4-9-4-9s0 6 4 9z" fill="#8a9b6e" />
            <path d="M12 15c0-7 5-10 5-10s0 7-5 10z" fill="#9fae7f" />
            <path
              d="M8 16h8l-1 6.5a1.3 1.3 0 0 1-1.3 1.1h-3.4A1.3 1.3 0 0 1 9.3 22.5L8 16z"
              fill="var(--accent)"
            />
            <rect x="7.3" y="14.3" width="9.4" height="2.4" rx="1" fill="var(--accent-strong)" />
          </svg>
          Кашпо<span> из массива</span>
        </a>
        <nav class="nav" aria-label="Основная навигация">
          <a href="/catalog">Каталог</a>
          <a href="/info">Доставка</a>
          <a href="/about">О нас</a>
          <a class="nav__cta" href="/custom">На заказ</a>
        </nav>
      </div>
    </header>

    <main>
      <slot />
    </main>

    <footer class="footer">
      <div class="container footer__inner">
        <p>© {new Date().getFullYear()} Мастерская кашпо. Кашпо из массива дерева на заказ.</p>
        <nav class="footer__links" aria-label="Служебные страницы">
          <a href="/info">Доставка и оплата</a>
          <a href="/privacy">Политика конфиденциальности</a>
          {TELEGRAM_LINK && (
            <a href={TELEGRAM_LINK} target="_blank" rel="noopener" data-metric="telegram_click">
              Написать в Telegram
            </a>
          )}
        </nav>
      </div>
    </footer>
  </body>
</html>

<style>
  .header {
    background: var(--bg-elevated);
    border-bottom: 1px solid var(--border);
  }

  .header__inner {
    display: flex;
    /* На узких экранах меню переносится под логотип целой строкой */
    flex-wrap: wrap;
    align-items: center;
    justify-content: space-between;
    gap: 0.375rem 1rem;
    padding-top: 0.75rem;
    padding-bottom: 0.75rem;
  }

  .logo {
    display: inline-flex;
    align-items: center;
    gap: 0.375rem;
    font-family: var(--font-display);
    font-size: var(--text-lg);
    font-weight: 700;
    color: var(--text);
  }

  .logo span {
    color: var(--accent);
  }

  .logo__mark {
    width: 1rem;
    height: 1rem;
    flex-shrink: 0;
  }

  .nav {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.25rem 0.875rem;
    font-size: var(--text-base);
  }

  .nav a {
    color: var(--text);
    font-weight: 600;
    /* Пункт меню не разрывается внутри себя на два слова */
    white-space: nowrap;
  }

  .nav a:hover {
    color: var(--accent-strong);
    text-decoration: none;
  }

  .nav a.nav__cta {
    padding: 0.3rem 0.875rem;
    font-size: 0.875rem;
    background: var(--accent);
    color: #fff;
    border-radius: 999px;
  }

  .nav a.nav__cta:hover {
    background: var(--accent-strong);
    color: #fff;
  }

  .footer {
    margin-top: 4rem;
    padding: 2rem 0;
    background: var(--bg-elevated);
    border-top: 1px solid var(--border);
    color: var(--text-muted);
    font-size: var(--text-sm);
  }

  .footer__inner {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .footer p {
    margin: 0;
  }

  .footer__links {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }

  .footer__links a {
    color: var(--text-muted);
  }

  @media (min-width: 768px) {
    .footer__inner {
      flex-direction: row;
      align-items: center;
      justify-content: space-between;
    }

    .footer__links {
      flex-direction: row;
      gap: 1.25rem;
    }
  }
</style>