services:
  pocketbase:
    build:
      context: .
      dockerfile: Dockerfile.pocketbase
    container_name: pb
    restart: unless-stopped
    volumes:
      - pb_data:/pb/pb_data
    ports:
      - "127.0.0.1:8090:8090"
    expose:
      - "8090"

  web:
    build:
      context: .
      dockerfile: Dockerfile.web
    container_name: web
    restart: unless-stopped
    environment:
      - HOST=0.0.0.0
      - PUBLIC_PB_PUBLIC_URL=${PUBLIC_PB_PUBLIC_URL}
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - TELEGRAM_CHAT_ID=${TELEGRAM_CHAT_ID}
      - TELEGRAM_USERNAME=${TELEGRAM_USERNAME}
      - MAX_LINK=${MAX_LINK}
      - YANDEX_METRIKA_ID=${YANDEX_METRIKA_ID}
    ports:
      - "127.0.0.1:4321:4321"
    expose:
      - "4321"

  caddy:
    image: caddy:2-alpine
    container_name: caddy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config

volumes:
  pb_data:
  caddy_data:
  caddy_config: